package com.qualcomm.ftcrobotcontroller.opmodes;

/**
 * This program prints "red" or "blue" based on the color sensor reading
 */

/**
 * This program detects Color and returns Red or Blue if one of the colors are sensed.
 */

import com.qualcomm.robotcore.eventloop.opmode.OpMode;
import com.qualcomm.robotcore.hardware.DcMotor;
import com.qualcomm.robotcore.util.Range;
import com.qualcomm.robotcore.hardware.IrSeekerSensor;

public class ColorSensorAutoTest {

    public void init() {
        /**
         * This function will be run only once.
         */
    }

    public void loop() {
        //This function will be run over and over again till terminated
    }

}