/*package com.qualcomm.ftcrobotcontroller.opmodes;


import com.qualcomm.robotcore.eventloop.opmode.OpMode;
import com.qualcomm.robotcore.hardware.<ClassName>;

public class <fileName> extends OpMode {

    <ClassName> <objectName>;

    @Override
    public void init() { //initializing function
        <objectName> = hardwareMap.<ClassName>.get(<nameSensorHere>);
    }

    @Override
    public void loop() { //loop function, do not clutter this you're smarter than that
        checkFunction();
    }

    public void checkFunction() {
        //this code will is for printing if or if not the sensor is being detected by the device or not
    }

    public void checkStrength() {
        //this code will very the strength of the connection, run checkStrength() if checkFuncion() is working like you want it to
    }

    public void stop() {

    } //stop code function
}*/